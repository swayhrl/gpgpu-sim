# W26 Next Steps

1. Add true power/energy field enablement if a supported AccelWattch/GPUWattch configuration is recovered for this simulator branch.
2. Continue workload blocker recovery without dropping placeholder rows.
3. Add correctness oracles where workloads currently only provide execution completion.
4. Promote inferred phase mapping only when kernel name/order/stat evidence supports exact alignment.
5. Convert the templates into reusable CI or nightly jobs once raw-log policy and timeout bounds are stable.
