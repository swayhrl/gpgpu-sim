paper: pcal
round: Phase 2
stage: cache_pressure_telemetry
status: complete
date: 2026-05-01
branch: hrl/paper/pcal-repro-v0

source_changed: true
experiments_run: true
workloads_run: [vecadd, rodinia_hotspot, rodinia_srad_v2, polybench_fdtd2d]
feature_off_unchanged: true
telemetry_on_unchanged: true
recommend_next: Phase 3
commit_recommended: true
suggested_commit: "pcal: add cache pressure telemetry"

cache_pressure_signal_summary:
  vecadd: "access=96, miss_rate=100%, classified_low=8, window_reset=8"
  rodinia_hotspot: "access=2576, miss_rate=100%, classified_low=262, window_reset=262"
  rodinia_srad_v2: "access=4352, miss_rate=97%, classified_low=512, window_reset=512"
  polybench_fdtd2d: "access=14450, miss_rate=74%, classified_low=1772, window_reset=1772"
  note: "All workloads classified_low due to miss_rate>50%; fdtd2d shows genuine cache reuse (74% miss rate)"

priority_signal_summary:
  classified_high: 0
  classified_low: non-zero for all workloads
  interpretation: "all tested workloads exceed 50% miss threshold → low priority; expected for tiny workloads with limited cache reuse"

hook_points:
  - "shader.cc process_cache_access() HIT/MISS branches (immediate path)"
  - "shader.cc l1_latency queue HIT/MISS branches (SM7_QV100 path)"

window_size_note: "window_size=8 used for tiny workloads; PCAL paper likely uses larger window; Phase 3 will use same size"

# 论文机制链（从 Phase 1 保留）
mechanism_chain:
  - name: miss-rate probe
    description: "per-warp L1D miss / access count in sliding window"
    stage: 02_telemetry
    hook_location: "shader.cc: process_cache_access() and l1_latency queue path"
    approximation: "window-based miss rate; window_size=8 for tiny workloads"
    status: complete
  - name: priority classification
    description: "miss_rate > threshold → low priority → bypass"
    stage: 03_would_change
    status: pending
  - name: L1 bypass gate
    description: "low-priority warp loads bypass L1D"
    stage: 04_minimal_mechanism
    status: pending

# 近似说明
approximation_notes:
  - "per-warp (not per-CTA) classification"
  - "simple miss/access ratio (not full CSI metric)"
  - "only L1D bypass (no L2 bypass)"
  - "binary priority (not multi-level)"
  - "fixed window size=8 (not adaptive; reduced for tiny workloads)"
