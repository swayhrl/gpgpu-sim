# Mascar M3 diagnostic microbenchmark

This workload alternates cold streaming loads with repeated hot-array loads to create memory pressure plus L1 reuse opportunities for non-owner hit-only diagnostics.
