# Trace Branch Policy

The trace branch is optional. Use it only when a paper needs kernel launch names/order for phase mapping or debugging.

Performance and energy runs should keep trace disabled unless trace evidence is explicitly required. The base infra branch intentionally has no simulator source modifications.

Do not add paper-specific mechanisms, configs, results, or raw logs to this trace branch.
