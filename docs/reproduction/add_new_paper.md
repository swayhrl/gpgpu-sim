# Add a New Paper

Create a paper-specific directory under `docs/papers`, define manifests/config matrices under `experiments/`, and keep raw logs outside git. Start with audit, then smoke, then bounded sweeps.
