# Raw Log Policy

Keep large raw logs and run directories out of git. Package them into `/workspace/tmp/*review_pack*.tar.gz` or raw-log archives and keep stable CSV/markdown summaries in the repo tree.
