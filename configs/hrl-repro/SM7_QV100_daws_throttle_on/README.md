# SM7_QV100_daws_throttle_on

DAWS minimal real throttling config.
gpgpu_enable_daws=1, telemetry=1, would_throttle=1, throttling=1.
footprint_threshold=32: blocks diverged warps when sum(footprint) > 32.
