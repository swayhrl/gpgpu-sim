# SM7_QV100_daws_noop_on

DAWS no-op on config.
gpgpu_enable_daws=1 but all sub-features (telemetry/would_throttle/throttling) = 0.
Used for: verifying master switch alone does not alter behavior.
paper_daws_enabled=1 in stats output; all behavior counters = 0.
